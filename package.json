{
	"name": "context-crawler",
	"version": "1.5.1",
	"type": "module",
	"bin": {
		"context-crawler": "./dist/src/cli.js"
	},
	"description": "Crawl websites to generate knowledge files from one or multiple URLs",
	"dependencies": {
		"commander": "^14.0.1",
		"cors": "^2.8.5",
		"crawlee": "^3.15.1",
		"dotenv": "^17.2.3",
		"express": "^5.1.0",
		"glob": "^11.0.3",
		"gpt-tokenizer": "^3.2.0",
		"inquirer": "^12.10.0",
		"playwright": "^1.56.1",
		"swagger-ui-express": "^5.0.1"
	},
	"devDependencies": {
		"@apify/tsconfig": "^0.1.1",
		"@types/cors": "^2.8.19",
		"@types/express": "^5.0.3",
		"@types/inquirer": "^9.0.9",
		"@types/node": "^24.9.1",
		"@types/swagger-ui-express": "^4.1.8",
		"prettier": "^3.6.2",
		"swagger-autogen": "^2.23.7",
		"ts-node": "^10.9.2",
		"typescript": "^5.9.3",
		"zod": "^4.1.12"
	},
	"scripts": {
		"preinstall": "npx playwright install",
		"start": "npm run start:dev && npx repomix@latest",
		"repomix": "npx repomix@latest",
		"check": "npx tsc --noEmit",
		"start:server": "NODE_ENV=development node swagger.js && npm run build && node dist/src/server.js",
		"start:server:prod": "npm run build && node dist/src/server.js",
		"start:cli": "cross-env NODE_ENV=development npm run build && node dist/src/cli.js",
		"start:dev": "cross-env NODE_ENV=development npm run build && node dist/src/config/main.js",
		"start:prod": "node dist/src/config/main.js",
		"test": "echo \"No test specified, please create tests.\" && exit 0",
		"build": "tsc",
		"fmt": "prettier --write .",
		"prettier:check": "prettier --check ."
	},
	"author": "It's not you it's me",
	"license": "ISC"
}
